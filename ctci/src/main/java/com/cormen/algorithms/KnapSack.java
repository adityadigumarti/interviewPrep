package com.cormen.algorithms;

public class KnapSack {

    /**
     * 
     * Total Weight W...Array Value and Weight for 1 to N elements
     * 
     * Create an Array array[n][W]
     * 
     * for(i = 1 to n)
     * for(w = 0 to W)
     * if(w[i] < w)
     * V[i,w] = max { V[i-1,w]
     * v{i} + v[i-1, w - w[i]]}
     * else
     * V[i,w] = v[i-1, w]
     * 
     * 
     */

}
